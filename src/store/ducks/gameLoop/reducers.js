import * as Actions from "./actions";
import initialState from "../../initialState";

export const gameLoopReducers = (state = initialState.player, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
